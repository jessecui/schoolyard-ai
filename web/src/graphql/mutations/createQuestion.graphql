mutation CreateQuestion($questionInput: QuestionInput!) {
  createQuestion(questionInput: $questionInput) {
    id
    createdAt
    updatedAt
    question
    choices
    answer
    teacher {
      firstName
      lastName
    }
    upVoteCount
    downVoteCount
    viewCount
  }
}
