mutation CreateQuestion($questionInput: QuestionInput!) {
  createQuestion(questionInput: $questionInput) {
    id
    createdAt
    updatedAt
    question
    choices
    answer
    teacher {
      firstName
      lastName
      photoUrl
    }
    upVoteCount
    downVoteCount
    viewCount
  }
}
