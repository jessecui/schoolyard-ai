mutation CreateQuestion($questionInput: QuestionInput!) {
  createQuestion(questionInput: $questionInput) {
    id
    createdAt
    updatedAt
    question
    choices
    answer
    creator {
      firstName
      lastName
      photoUrl
    }
    upVoteCount
    downVoteCount
    viewCount
  }
}
