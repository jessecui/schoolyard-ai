query Question($id: Int!) {
  question(id: $id) {
    id
    question
    questionType
    choices
    answer
    teacherId
    teacher {      
      firstName
      lastName
      photoUrl
    }
    sentence {
      id
      text
      subjects
      teacherId
      teacher {
        firstName
        lastName
        photoUrl
      }
      children {
        id
        text
      }
      userVoteType
      upVoteCount
      downVoteCount
      viewCount
      createdAt
    }
    subjects
    upVoteCount
    downVoteCount
    userVoteType
    viewCount
    createdAt
    updatedAt
  }
}
